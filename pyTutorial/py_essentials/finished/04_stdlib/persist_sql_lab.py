"""LAB:

* Create a simple table with two rows of hostnames and accesses.
* Borrow code from the logparsing for reading the accessing hosts.
* Store the counts of all hosts in the db (not just the top 10).

"""